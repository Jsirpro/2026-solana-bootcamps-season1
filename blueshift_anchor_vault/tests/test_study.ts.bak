import * as anchor from "@coral-xyz/anchor";
import BN from "bn.js";
import { PublicKey, SystemProgram,LAMPORTS_PER_SOL} from "@solana/web3.js";
import { assert } from "chai";

describe("test for vault",() =>{
    //provider 里是connection 和 wallet
    //创建provider，测试用provider去连链，交易，.env就是从环境变量创建provider
    const provider = anchor.AnchorProvider.env();
    anchor.setProvider(provider);
    //定义程序
    //属性名（BlueshiftAnchorVault）来自 Anchor.toml，再做一次
    //命名风格转换：blueshift_anchor_vault => BlueshiftAnchorVault
    const program = anchor.workspace.BlueshiftAnchorVault;
    //用环境变量里的wallet去签名交易
    const signer = provider.wallet;
    //创建Publickey类型的vault,暂时没有值
    let vaultPda:PublicKey

    /**在所有测试前执行cheatcode，初始化signer的余额*/
    before(async () =>{
        const richBalance = 10 * LAMPORTS_PER_SOL; //10 SOL

        
    });
});